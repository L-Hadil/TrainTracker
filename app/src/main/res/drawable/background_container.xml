<shape xmlns:android="http://schemas.android.com/apk/res/android"
    android:shape="rectangle">

    <!-- Couleur de fond : blanc semi-transparent pour voir un peu le train derrière -->
    <solid android:color="@color/semiTransparentWhite" />

    <!-- Coins arrondis -->
    <corners android:radius="24dp" />

    <!-- Marges internes (optionnel) -->
    <padding
        android:left="16dp"
        android:top="16dp"
        android:right="16dp"
        android:bottom="16dp" />
</shape>
